---
- name: Configure all web servers
  hosts: webservers
  become: yes

  roles:
    - common
    - docker
    - app

  post_tasks:
    - name: Print server summary
      debug:
        msg: "Server {{ inventory_hostname }} ({{ ansible_host }}) fully configured!"
